apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-server
  namespace: revup-backend
  labels:
    app: eureka-server
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eureka-server
      tier: backend
  template:
    metadata:
      labels:
        app: eureka-server
        tier: backend
    spec:
      containers:
      - name: eureka-server
        image: anjed11/revup-eureka-server:v1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8761
          name: http
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: eureka-server-service
  namespace: revup-backend
  labels:
    app: eureka-server
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8761
    targetPort: 8761
    protocol: TCP
    name: http
  selector:
    app: eureka-server
    tier: backend
