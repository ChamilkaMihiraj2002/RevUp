apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: revup-backend
  labels:
    app: appointment-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-service
      tier: backend
  template:
    metadata:
      labels:
        app: appointment-service
        tier: backend
    spec:
      containers:
      - name: appointment-service
        image: anjed11/appointment-service:v1.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8086
          name: http
        env:
        - name: APPOINTMENT_SERVICE_PORT
          value: "8086"
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: backend-config
              key: SPRING_PROFILES_ACTIVE
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            secretKeyRef:
              name: database-credentials
              key: APPOINTMENT_SERVICE_JDBC_URL
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: database-credentials
              key: DB_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-credentials
              key: DB_PASSWORD
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: backend-config
              key: EUREKA_SERVER_URL
        - name: SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE
          valueFrom:
            configMapKeyRef:
              name: backend-config
              key: HIKARI_MAXIMUM_POOL_SIZE
        - name: SPRING_JPA_HIBERNATE_DDL_AUTO
          valueFrom:
            configMapKeyRef:
              name: backend-config
              key: JPA_DDL_AUTO
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"

---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: revup-backend
  labels:
    app: appointment-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8086
    targetPort: 8086
    protocol: TCP
    name: http
  selector:
    app: appointment-service
    tier: backend
